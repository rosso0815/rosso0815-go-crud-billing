package ui

templ pagination(a []PaginationLink) {
	<nav>
		<ul class="pagination">
			for _, item := range a {
				<li class="page-item"><a class="page-link text-dark" href={ item.URL }>{ item.Text }</a></li>
			}
		</ul>
	</nav>
}

// --- EOF
